{
  "name": "memoryloupe",
  "publisher": "plharraud",
  "displayName": "memoryloupe - gcc mapfile codelenses",
  "description": "codelenses about gcc compiled executable symbol size and stack usage",
  "keywords": [
    "symbol",
    "size",
    "stack",
    "usage",
    "mapfile"
  ],
  "preview": true,
  "license": "SEE LICENSE IN LICENSE",
  "homepage": "https://github.com/plharraud/memoryloupe/blob/dev/README.md",
  "repository": {
    "type": "git",
    "url": "https://github.com/plharraud/memoryloupe"
  },
  "bugs": {
    "url": "https://github.com/plharraud/memoryloupe/issues"
  },
  "extensionKind": [
    "workspace"
  ],
  "pricing": "Free",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "vscode": "^1.86.0"
  },
  "categories": [
    "Visualization",
    "Debuggers"
  ],
  "activationEvents": [
    "onLanguage:c",
    "onLanguage:cpp"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": []
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test",
    "vsix": "npx vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.86.0",
    "@typescript-eslint/eslint-plugin": "^8.22.0",
    "@typescript-eslint/parser": "^8.22.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^3.2.2",
    "esbuild": "^0.24.2",
    "eslint": "^9.19.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.3"
  }
}